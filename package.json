{
  "name": "react-training",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "fromScratch": "npm run clean && mkdir build && npm run copyHtml",
    "predev": "npm install",
    "dev": "npm run fromScratch && webpack-dev-server --content-base build --open",
    "prebuild": "npm install",
    "build": "npm run fromScratch && webpack",
    "clean": "rm -rf build",
    "copyHtml": "cp src/app/index.html build/index.html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "devDependencies": {
    "babel-core": "^6.23.1",
    "babel-loader": "^6.3.2",
    "babel-preset-es2015": "^6.22.0",
    "babel-preset-react": "^6.23.0",
    "webpack": "^2.2.1",
    "webpack-dev-server": "^2.4.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/p-equis/react-training.git"
  }
}
